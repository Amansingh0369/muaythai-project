# Product

This is a Muay Thai project consisting of two Next.js web applications:

- **web** (`apps/web`, port 3000): Public-facing web app
- **dashboard** (`apps/dashboard`, port 3001): Internal dashboard for metrics and management (users, revenue, sessions, reports)

Both apps share a common UI component library and utility functions via internal packages.
